# W = write and create a new file Read
# r = read
# a = append the data into the file 
# W+ = to read and write data 
# R+ = read and write ,revious data in the file will not deleted
# A+ = append and read the data of file (create file does not exist)
# x = create a new file
# wb ,ab , a+b, r+b, w+b, are the mode of binary
